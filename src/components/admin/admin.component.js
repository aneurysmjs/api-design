import template from './admin.html';
import controller from './admin.controller';

import './admin.styl';

let adminComponent = {
  template,
  controller,
  bindings: {}
};

export default adminComponent;
